apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Fedodo.Chart.fullname" . }}-test-connection"
  labels:
    {{- include "Fedodo.Chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Fedodo.Chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
